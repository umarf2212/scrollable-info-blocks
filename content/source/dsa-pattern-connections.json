{
  "1": "Model pattern: staircase search in a row-and-column-sorted matrix. The top-right corner is useful because every comparison can discard one complete row or column.",
  "2": "Model pattern: binary search on the partition between two sorted arrays. The twist is searching only the shorter array and treating missing partition values as infinities.",
  "3": "Model pattern: binary search on the answer. Instead of locating an index, test a candidate value by counting how many matrix entries are less than or equal to it.",
  "4": "Model pattern: binary search for the last feasible value. Here feasibility is mid² ≤ A, and division is used to avoid overflow from multiplying mid by itself.",
  "5": "Model pattern: binary search on a rotated sorted array. This implementation first isolates the rotation peak, then searches the two ordinary sorted halves.",
  "6": "Model pattern: binary search on a monotone answer. A window length is feasible only when every window sum stays within B; non-negative values make that predicate monotone.",
  "7": "Model pattern: kth number in a union of multiples. Inclusion-exclusion counts values divisible by B or C, and binary search finds the first value reaching rank A.",
  "8": "Model pattern: minimize the maximum load with binary search on the answer. The problem-specific constraint is that books must remain contiguous, so feasibility is checked greedily in order.",
  "9": "Model pattern: maximize the minimum allocation. A candidate packet size is feasible when the populations can collectively produce at least B equal packets.",
  "10": "Model pattern: multiple pointers over sorted inputs. The current cost is max−min, so only the pointer at the current minimum can move toward a better range.",
  "11": "Model pattern: three sorted pointers minimizing a range. The apparent pairwise-absolute-difference objective collapses to max(current)−min(current).",
  "12": "Model pattern: partition three known values. This source uses counting and overwrite rather than the one-pass Dutch National Flag version.",
  "13": "Model pattern: two-sum on sorted arrays. The twist is preserving the required index tie-break while one pointer moves from each end.",
  "14": "Model pattern: fix one value, then solve a sorted two-pointer problem for the remaining pair. The target is closest sum rather than exact sum.",
  "15": "Model pattern: count all feasible pairs in a sorted array without enumerating them. Once A[i]·A[j] is valid, every partner between i and j is valid too.",
  "16": "Model pattern: last-seen index in a hash map. The crucial twist is that minimum distance requires the latest index, not the first index retained by the captured code.",
  "17": "Model pattern: equal prefix sums delimit a zero-sum subarray. Keeping first and last positions converts that identity into the longest valid slice.",
  "18": "Model pattern: fixed-size sliding window plus a frequency map. Distinct count changes only when an outgoing count reaches zero or an incoming value first appears.",
  "19": "Model pattern: repeated prefix sum implies a zero-sum interval. A set is enough because the problem asks only whether such an interval exists.",
  "20": "Model pattern: hash-set expansion of a consecutive run. Marking numbers visited prevents the same run from being explored from every member.",
  "21": "Model pattern: palindrome feasibility from frequency parity. The only special case is allowing one odd-frequency character as the center.",
  "22": "Model pattern: hash every derived value and reject duplicates. The twist is that products come from every consecutive digit slice, including slices containing zero.",
  "23": "Model pattern: frequency counting with a custom order. Emit values named by B first, then sort only the values B did not constrain.",
  "24": "Model pattern: test geometric completion with a point set. Choosing a diagonal determines the other two corners of an axis-aligned rectangle in O(1) lookup time.",
  "25": "Model pattern: count choices around each pivot. A point is the right-angle vertex, with independent horizontal and vertical partner counts.",
  "26": "Model pattern: divisibility of character frequencies. The string is A repeated copies exactly when every character count can be divided evenly by A.",
  "27": "Model pattern: group points by normalized slope from an anchor. GCD reduction and separate duplicate-point handling make equivalent lines share a key.",
  "28": "Model pattern: range fingerprinting with prefix sums. The twist is order-insensitive comparison, but a single summed hash is probabilistic rather than a proof of equal multisets.",
  "29": "Model pattern: multiset intersection. Each common value is emitted min(countA, countB) times, so duplicates are preserved correctly.",
  "30": "Model pattern: cyclic shifts become substring matches in B+B. Using B+B[:-1] exposes exactly N rotation starts, and KMP counts overlaps in linear time.",
  "31": "Model pattern: greedy lexicographic prefix construction. After taking A[0], only A characters smaller than B[0] can improve the prefix before appending B[0].",
  "32": "Model pattern: two-pointer palindrome validation. The problem asks for exactly one replacement, so a perfect even-length palindrome is handled differently from an odd-length one.",
  "33": "Model pattern: longest palindromic prefix via KMP/LPS. Concatenating A with reverse(A) turns the needed prefix length into the final prefix-function value.",
  "34": "Model pattern: count overlapping pattern occurrences. Advancing by one position after a match is what allows overlaps instead of skipping them.",
  "35": "Model pattern: slow/fast pointer to locate the middle node. Keeping the predecessor lets the code delete the middle without a second traversal.",
  "36": "Model pattern: reverse a linked list in fixed-size groups. The difficult part is reconnecting the previous group's tail to the new group head and the reversed tail to the remainder.",
  "37": "Model pattern: iterative linked-list reversal with prev/current/next pointers. Saving next before rewiring is the invariant that prevents losing the remaining list.",
  "38": "Model pattern: tortoise-and-hare traversal. Advancing fast twice for every slow step positions slow at the requested middle in one pass.",
  "39": "Model pattern: reverse only a sublist. The twist is preserving both outside boundaries so the reversed segment can be spliced back without losing the head or tail.",
  "40": "Model pattern: implement a mutable singly linked list behind a fixed function API. Position validation and head operations are the problem-specific edge cases.",
  "41": "Model pattern: two pointers separated by N nodes. When the lead pointer reaches the end, the trailing pointer is immediately before the node to remove.",
  "42": "Model pattern: compress adjacent duplicates in a sorted list. Sorting guarantees every duplicate is next to the value that should be retained.",
  "43": "Model pattern: Floyd cycle detection followed by cycle-entry recovery. Unlinking needs the node just before the entry, not merely the entry itself.",
  "44": "Model pattern: reorder from both ends. This source stores nodes in a stack, trading O(N) memory for simple alternating tail extraction.",
  "45": "Model pattern: merge sort adapted to linked lists. Splitting uses slow/fast pointers, while merging must rewire nodes rather than index into an array.",
  "46": "Model pattern: merge two sorted sequences with a moving tail pointer. Linked lists make the merge about pointer ownership and remaining-tail attachment.",
  "47": "Model pattern: monotonic decreasing stack for next-greater elements. Indices stay unresolved until a strictly larger value appears to their right.",
  "48": "Model pattern: postfix evaluation with an operand stack. Operand order matters for subtraction and division: the first pop is the right operand.",
  "49": "Model pattern: contribution counting. Every element appears in (i+1)(N−i) subarrays, avoiding explicit generation of all subarrays.",
  "50": "Model pattern: monotonic-stack contribution counting for subarray minima and maxima. Duplicate tie rules must be asymmetric so equal values are not counted twice.",
  "51": "Model pattern: nearest-smaller boundaries around every histogram bar. Each bar becomes the limiting height across the widest interval before a smaller bar.",
  "52": "Model pattern: monotonic increasing stack for previous-smaller values. Larger or equal values are popped because they can never be the nearest valid answer for future elements.",
  "53": "Model pattern: stack simulation of passes and undo operations. A numeric command pushes a new holder; an undo command pops the latest holder.",
  "54": "Model pattern: frequency stack. Ordering is by highest frequency first and, within a frequency, most recent insertion.",
  "55": "Model pattern: insertion sort using an auxiliary stack. Elements are temporarily moved aside until the popped value can be inserted at its sorted position.",
  "56": "Model pattern: encode the running minimum inside one stack. Each push stores enough information for getMin to stay O(1) without scanning.",
  "57": "Model pattern: shunting-yard conversion from infix to postfix. Precedence and associativity decide which operators leave the stack before the current one is pushed.",
  "58": "Model pattern: use a stack to inspect what appears inside each parenthesis pair. A pair is redundant when it encloses no operator.",
  "59": "Model pattern: propagate signs through nested parentheses. Each variable is reduced to its effective sign, then the two normalized expressions are compared.",
  "60": "Model pattern: adjacent-pair cancellation with a stack. A matching top means the pair disappears; otherwise the character becomes a new unresolved top.",
  "61": "Model pattern: the same shunting-yard conversion as node 57. This duplicate is useful for noticing the same exponent-associativity caveat in both captured implementations.",
  "62": "Model pattern: reduce each binary-matrix row to a histogram, then reuse largest-rectangle-in-histogram. Vertical heights carry all rectangles ending at the current row."
}
